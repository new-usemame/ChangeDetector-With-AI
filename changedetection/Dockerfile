# changedetection.io service
# This pulls the official image and configures it for Railway

FROM ghcr.io/dgtlmoon/changedetection.io:latest

# Railway will set PORT dynamically
ENV PORT=5000

# Expose the port
EXPOSE 5000

# The base image already has the correct entrypoint
